# Backups y recuperación

Directorio reservado para RPO/RTO, `pg_dump`, retención, checksums, `pg_restore` y ejercicios de recuperación de V18. Un backup no se considerará válido sin una restauración comprobada.

